require 'spec_helper'

require 'net/http'

describe Lab4::LiveStreamConnection do

  it "requests an access token, connects to the live stream endpoint, and counts the number of unique visitor IDs" do
    # insert your test code here
  end

end
